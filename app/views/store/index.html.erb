`<div class="row container">
  <div class="large-12 small-12 columns">
  <div id="cart"> 
    <%= render 'cart', locals: {:@cart => @cart} %>
  </div>
    <p> List all products </p>
    <ul class="clearing-thumbs" data-clearing>

    <% @products.each do |product| %>
      <li><a href="<%= product.image_url %>" data-caption="<%= product.name %>" data-clearing-interchange="[#{product.image_url}, (default)], [#{product.image_url}, (large)]"><img src="#{product.image_url}"><%= product.name %></a></li><br>
      <p>
      <%= link_to 'Add to Cart', add_to_cart_path(current_cart.id, product.id), data: {cart_id: "#{current_cart.id}", product_id: "#{product.id}"}, class: 'add-to-cart' %>
      </p>
    <% end %>
    </ul>
    <%= render 'form' %>
  </div>
</div>