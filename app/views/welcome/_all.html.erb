<% if @result %>

  <div class="allresults">
      <% @result.each do |score, name| %>
          <div class="eachresult1">
          		<div class="name1">
          		<%="Go to: #{name}" %>
           		</div>
           		<div class="location1">
           		<%= "@ #{Event.find_by_name(name).location}" %>
           		</div>
           		<div class="time1">
           		<%= "@ #{Event.find_by_name(name).time}" %>
           		</div>
              	<div class="matchscore1">
                Relevance score: 
                <a class="score1"><%= score %></a>
              	</div>
         	</div>
      <% end %>
  </div><br>
   
<% end %>
